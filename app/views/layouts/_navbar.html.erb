<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <%= link_to "WMT Results", 'https://www.youtube.com/@bridgelesson', target: "_blank", class: "navbar-brand fw-bold" %>

    <div class="ms-auto d-flex align-items-center gap-2">
      <% if authenticated? %>
        <span class="navbar-text text-white-50 small d-none d-sm-inline">
          Signed in as <%= Current.session.user.email_address %>
        </span>
        <%= button_to "Sign out", session_path, method: :delete, class: "btn btn-outline-light btn-sm" %>
      <% else %>
        <%= link_to "Sign in", new_session_path, class: "btn btn-outline-light btn-sm" %>
      <% end %>
    </div>
  </div>
</nav>
