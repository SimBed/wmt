<% content_for :title, "New result" %>

<div class="mb-4">
  <h1 class="h2 mb-1">New result</h1>
  <p class="text-muted mb-0">Add a new score and position.</p>
</div>

<div class="card border-0 shadow-sm">
  <div class="card-body">
    <%= render "form", result: @result %>
  </div>
</div>
