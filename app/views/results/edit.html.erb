<% content_for :title, "Edit result ##{@result.number}" %>

<div class="mb-4">
  <h1 class="h2 mb-1">Edit result #<%= @result.number %></h1>
  <p class="text-muted mb-0">Update this result.</p>
</div>

<div class="card border-0 shadow-sm">
  <div class="card-body">
    <%= render "form", result: @result %>
  </div>
</div>
